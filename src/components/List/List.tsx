import React from "react";

import { Card } from "../../types";

interface ListProps {
  cards: Card[];
}

const List: React.SFC<ListProps> = () => {
  return <div>List`</div>;
};

export default List;
